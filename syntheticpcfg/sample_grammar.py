#sample_grammar.py

import argparse


parser = argparse.ArgumentParser(description='Create a randomly generated PCFG')

parser.add_argument("outputfilename", description="File where the resulting PCFG will be stored")

parser.add_argument("--terminals", description="Number of terminals", def)


